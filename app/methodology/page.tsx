import type { Metadata } from "next";
export const metadata:Metadata={title:"Methodology",description:"How Plumbline sources, verifies, and publishes election dates."};
const steps=[["01","Source","We begin with election offices, secretaries of state, statutes, and official calendars—not aggregators."],["02","Structure","Records are normalized into a strict schema with explicit dates, offices, confidence, and retrieval metadata."],["03","Review","A human checks each source and marks the record verified before it can reach the public build."],["04","Publish","The site, JSON, CSV, and calendar feeds are generated from the same reviewed record."]];
export default function Methodology(){return <article className="mx-auto max-w-5xl px-5 py-16 lg:px-8"><p className="rule-label">Standards desk</p><h1 className="mt-3 text-balance font-serif text-5xl font-bold">Accuracy over completeness.</h1><p className="mt-6 max-w-3xl text-xl leading-relaxed text-muted-foreground">An empty field is more honest than a guessed deadline. Plumbline publishes only what its sources can support, and keeps confidence visible to readers.</p><div className="mt-14 border-t">{steps.map(([n,title,text])=><section key={n} className="grid gap-4 border-b py-8 md:grid-cols-[5rem_12rem_1fr]"><span className="font-mono text-secondary">{n}</span><h2 className="font-serif text-2xl font-bold">{title}</h2><p className="text-muted-foreground">{text}</p></section>)}</div><section className="mt-14 border-l-4 border-secondary bg-card p-7"><h2 className="font-serif text-2xl font-bold">Confidence language</h2><p className="mt-3 text-muted-foreground"><strong className="text-foreground">Confirmed official</strong> means the date appears on the election office&apos;s own published material. Provisional labels are used when a date relies on secondary reporting or statutory inference.</p></section></article>}
