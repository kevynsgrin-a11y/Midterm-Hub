import type { Metadata } from "next";
import Link from "next/link";
import { StateCartogram } from "@/components/data-visuals";
import { elections, states, stateHref } from "@/lib/data";
export const metadata:Metadata={title:"State election calendars",description:"Browse verified 2026 election dates and voter deadlines by state."};
export default function StatesPage(){return <div className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><p className="rule-label">National desk / state index</p><h1 className="mt-3 font-serif text-5xl font-bold">Election calendars by state</h1><p className="mt-4 max-w-2xl text-lg text-muted-foreground">Start with your state. Each desk gathers election events, deadlines, offices, and source citations in one place.</p><div className="mt-12 border bg-card p-5 md:p-8"><StateCartogram/></div><div className="mt-14 grid border-l border-t sm:grid-cols-2 lg:grid-cols-3">{states.map(([code,name])=>{const count=elections.filter(e=>e.state===code).length;return <Link key={code} href={stateHref(code)} className="group flex items-center justify-between gap-4 border-b border-r p-5 hover:bg-card"><span><strong className="font-serif text-xl">{name}</strong><span className="mt-1 block text-sm text-muted-foreground">{count} verified {count===1?"record":"records"}</span></span><span className="font-mono font-bold text-secondary">{code}</span></Link>})}</div></div>}
