import type { Metadata } from "next";
import { Database, FileJson2, Sheet, CalendarDays } from "lucide-react";
import { data, elections } from "@/lib/data";
export const metadata:Metadata={title:"Open election data",description:"Download and inspect Midterm Watch election records and provenance metadata."};
export default function DataPage(){return <div className="mx-auto max-w-6xl px-5 py-16 lg:px-8"><p className="rule-label">Open data / {data.edition}</p><h1 className="mt-3 font-serif text-5xl font-bold">Built to be inspected.</h1><p className="mt-5 max-w-2xl text-lg text-muted-foreground">The same reviewed records that power this site are available in portable formats. Every row retains its source URL and confidence.</p><div className="mt-12 grid gap-5 md:grid-cols-3">{[[FileJson2,"JSON","Structured records for apps and analysis."],[Sheet,"CSV","Flat rows for spreadsheets and reporting."],[CalendarDays,"ICS","Calendar feeds for election reminders."]].map(([Icon,title,text])=><section key={title as string} className="border bg-card p-6"><Icon className="text-secondary"/><h2 className="mt-5 font-serif text-2xl font-bold">{title as string}</h2><p className="mt-2 text-sm text-muted-foreground">{text as string}</p><p className="mt-6 rule-label">Generated during release builds</p></section>)}</div><div className="mt-12 grid gap-px bg-border md:grid-cols-3">{[[elections.length,"records"],[new Set(elections.map(e=>e.state)).size,"jurisdictions"],[new Set(elections.map(e=>e.source_url)).size,"source documents"]].map(([n,l])=><div key={l} className="bg-primary p-7 text-primary-foreground"><strong className="font-serif text-4xl text-secondary">{n}</strong><span className="block rule-label text-primary-foreground/70">{l}</span></div>)}</div><div className="mt-12 flex gap-4 border-l-4 border-secondary bg-card p-6"><Database className="shrink-0 text-secondary"/><p className="text-sm text-muted-foreground">Generated {new Date(data.generated_at).toLocaleString("en-US",{dateStyle:"long",timeStyle:"short"})}. Downloads are generated by the audited Python release pipeline.</p></div></div>}
